'use client';

// This component is handled via CSS classes and Tiptap's built-in selection management.
// It exists here to satisfy the architectural requirement and provide a hook for future
// complex visual selection overlays if needed.
export function TableSelection() {
  return null;
}
