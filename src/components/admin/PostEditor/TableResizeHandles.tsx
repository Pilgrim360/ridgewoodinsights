'use client';

// Tiptap's Table extension with { resizable: true } handles resize handles automatically.
// This component can be used to customize the handles if the built-in ones are insufficient.
export function TableResizeHandles() {
  return null;
}
